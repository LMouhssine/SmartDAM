FLASK_SECRET_KEY=change-me
DATABASE_URL=sqlite:///smartdam.db

# Admin account (single account, no user table). Generate the hash with:
#   python -c "from werkzeug.security import generate_password_hash; print(generate_password_hash('your-password'))"
ADMIN_USERNAME=admin
ADMIN_PASSWORD_HASH=
MAX_CONTENT_LENGTH=20971520
THUMBNAIL_MAX_SIZE=640
UPLOAD_FOLDER=uploads
LOG_LEVEL=INFO

# Local by default. Switch to true to store new files in Azure Blob Storage.
# The target container must allow public blob read access because SmartDAM stores the direct blob URL.
USE_AZURE_STORAGE=false
AZURE_STORAGE_CONNECTION_STRING=
AZURE_STORAGE_CONTAINER=smartdam-images

# Hugging Face image analysis
HUGGINGFACE_API_TOKEN=
HUGGINGFACE_CLASSIFICATION_MODEL=microsoft/resnet-50
HUGGINGFACE_DETECTION_MODEL=facebook/detr-resnet-50
HUGGINGFACE_CAPTION_MODEL=
HUGGINGFACE_TIMEOUT=20
HUGGINGFACE_MAX_TAGS=8
